!pip install langchain_huggingface

from langchain_huggingface import HuggingFaceEmbeddings

embeddings_model = HuggingFaceEmbeddings(model_name=model_name)

embedded_query = embeddings_model.embed_documents([
    "hi",
    "hello",
    "how are you"
])

len(embedded_query) -> 3

len(embedded_query[0]),len(embedded_query[1]),len(embedded_query[2])

-> (768, 768, 768)
